<project>
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.codehaus.mojo</groupId>
    <artifactId>mojo-sandbox</artifactId>
    <version>2</version>
  </parent>

  <artifactId>repository-purge-maven-plugin</artifactId>
  <version>1.0-SNAPSHOT</version>
  
  <packaging>maven-plugin</packaging>
  
  <name>Repository Purge Plugin</name>
  
  <description>
    Purges a project's dependencies 
    from the repository, and optionally re-resolves them. To handle the
    case where a project depends on a third-party, manually installed dependency,
    this plugin also allows a list of groupId:artifactId exclude elements in
    its configuration.
  </description>

  <dependencies>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-plugin-api</artifactId>
      <version>2.0</version>
    </dependency>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-project</artifactId>
      <version>2.0</version>
    </dependency>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-artifact</artifactId>
      <version>2.0</version>
    </dependency>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-artifact-manager</artifactId>
      <version>2.0</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-plugin-plugin</artifactId>
        <configuration>
          <goalPrefix>purge-repo</goalPrefix>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
