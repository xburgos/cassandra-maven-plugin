<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.codehaus.mojo.patch.it</groupId>
    <artifactId>it-project-parent</artifactId>
    <version>1</version>
  </parent>
  
  <artifactId>package-patches-directory</artifactId>
  <name>package-patches-directory</name>
  
  
  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>patch-maven-plugin</artifactId>
        <version>testing</version>
        
        <executions>
          <execution>
            <id>resolve-patches</id>
            <phase>initialize</phase>
            <goals>
              <goal>resolve-patches</goal>
            </goals>
            <configuration>
              <skipResolve>true</skipResolve>
            </configuration>
          </execution>
          <execution>
            <id>package-patches</id>
            
            <!--  putting it ahead of generate-sources, since this should patch any model files, etc. -->
            <phase>package</phase>
            
            <goals>
              <goal>package-patches</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
