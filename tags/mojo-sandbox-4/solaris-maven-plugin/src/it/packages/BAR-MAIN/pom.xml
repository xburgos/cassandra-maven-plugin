<?xml version="1.0" encoding="UTF-8"?>
<project>
	
  <parent>
    <groupId>foo.bar</groupId>
    <artifactId>BAR-ALL</artifactId>
    <version>1.0</version>
  </parent>

  <modelVersion>4.0.0</modelVersion>
  
  <groupId>foo.bar</groupId>
  <artifactId>BAR-MAIN</artifactId>
  <version>1.0</version>
  <name>BAR MAIN Package</name>
  
  <description>Main package for foo::bar software</description>

  <packaging>solaris</packaging>

  <dependencies>
    <dependency>
      <groupId>foo.bar</groupId>
      <artifactId>foo-bar</artifactId>
      <version>1.0</version>
    </dependency>
  </dependencies>

  <!-- example how to include a webapp to the package -->
  <!-- 
  <build>	  
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>dependency-maven-plugin</artifactId>
        <executions>
          <execution>
            <phase>process-resources</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                <groupId>foo.bar</groupId>
                <artifactId>bar-webapp</artifactId>
                <version>1.0</version>
                <type>war</type>
               </artifactItem>
             </artifactItems>
             <outputDirectory>${project.build.outputDirectory}/ROOT/opt/tomcat/webapp</outputDirectory>
           </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  -->
</project>