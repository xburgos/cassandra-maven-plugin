import java.io.*;

File testFile = new File( basedir, "target/package-patches-directory-1-patches.zip" );

System.out.println( "Does " + testFile + " exist? " + testFile.exists() );

return testFile.exists();
