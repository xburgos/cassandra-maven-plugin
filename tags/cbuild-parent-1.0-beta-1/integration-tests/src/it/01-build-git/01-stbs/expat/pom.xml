<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.codehaus.mojo.cbuild-parent.it.stbs</groupId>
  <artifactId>expat</artifactId>
  <version>1.95.8</version>

  <parent>
    <groupId>org.codehaus.mojo.cbuild-parent.it</groupId>
    <artifactId>it-project-parent</artifactId>
    <version>1</version>
  </parent>

  <build>
    <extensions>
      <extension>
        <groupId>org.apache.maven.wagon</groupId>
        <artifactId>wagon-file</artifactId>
        <!-- specify the version on extensions or bad things happen -->
        <version>1.0-beta-5</version>
      </extension>
    </extensions> 
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>wagon-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>download-stb</id>
            <phase>initialize</phase>
            <goals>
              <goal>download-single</goal>
            </goals>
            <configuration>
              <!--serverId>when downloading, match this to your ~/.m2/settings.xml</serverId-->
              <url>file://@project.build.directory@/tmp</url>
              <fromFile>${pkgName}.${srcURLSuffix}</fromFile>
              <toDir>${project.build.directory}</toDir>
              <!--toDir>${project.build.directory}</toDir-->
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-artifacts</id>
            <phase>package</phase>
            <goals>
              <goal>attach-artifact</goal>
            </goals>
            <configuration>
              <artifacts>
                <artifact>
                  <file>${project.build.directory}/${pkgName}.${srcURLSuffix}</file>
                  <type>${srcURLSuffix}</type>
                  <!--classifier>noarch</classifier-->
                </artifact>
              </artifacts>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
